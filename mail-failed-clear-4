#!/bin/bash

CURRENT_DIR="$(pwd)"

find "$CURRENT_DIR" -type f -size -10M -print0 | \
xargs -0 grep -IlE \
"Mail delivery failed: returning message to sender|Mail delivery deferred: returning message to sender|Mail failure|Message Delivery Failure|Undeliverable:" | \
xargs -r rm -f
